{
  "name": "whosenxt-backend",
  "version": "1.0.0",
  "description": "Backend server for WHOSENXT Stripe integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18 <21"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "stripe": "^12.0.0"
  }
}
{
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "npx serve -s frontend/build"
  },
  "dependencies": {
    "serve": "^14.0.1"
  }
}
